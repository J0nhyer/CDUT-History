<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cdut.ai.mapper.AcademicUniverseMapper">

    <select id="findByUniversityId" parameterType="string" resultType="org.cdut.ai.model.AcademicUniverse">
        SELECT * FROM academic_universe WHERE university_id = #{universityId}
    </select>

</mapper>

