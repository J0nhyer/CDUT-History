# npm配置文件 - 解决依赖安装问题

# 使用淘宝镜像源（国内速度更快）
registry=https://registry.npmmirror.com

# 严格的SSL检查（如果有SSL问题可以设为false，但不推荐）
strict-ssl=true

# 自动安装peer dependencies
legacy-peer-deps=false

# 超时时间设置为60秒
timeout=60000

# 保存时使用精确版本
save-exact=false

# 安装时显示进度
progress=true

# 日志级别
loglevel=warn
